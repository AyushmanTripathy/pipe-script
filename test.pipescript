# factorial
set n 10

log | call switch $n
function switch $a
  switch $a
    case 10
      log 'equal 1'
      break
    case 10
      log 'equal 10'
      return $a
    default
      log 'default' 
    default
      log 'default'
